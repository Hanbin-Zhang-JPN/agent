/* ============================================================
 * 文件：05-array/array.c
 * 主题：数组
 * 学习点：一维数组 / 下标访问 / 循环遍历 / 二维数组
 * ============================================================ */

#include <stdio.h>                 // 引入标准输入输出库

int main(void)
{
    // 1. 一维数组：声明时给出大小，用花括号初始化
    int scores[5] = {90, 85, 78, 92, 88};   // 5 个元素，下标从 0 开始

    // 2. 通过下标访问元素
    printf("第一个成绩：%d\n", scores[0]);  // 下标 0 是第一个元素
    printf("第三个成绩：%d\n", scores[2]);  // 下标 2 是第三个元素

    // 3. 用 for 循环遍历数组：下标 0 到 4
    int sum = 0;                   // 总分累加器，先归零
    for (int i = 0; i < 5; i++) {  // i 依次取 0、1、2、3、4
        sum += scores[i];          // 累加每个元素
    }
    printf("总分：%d\n", sum);     // 输出累加结果

    // 4. 修改数组元素：和普通变量一样赋值
    scores[0] = 100;               // 把第一个成绩改成 100
    printf("修改后第一个成绩：%d\n", scores[0]);

    // 5. 二维数组：可以理解为"表格"，[行][列]
    int table[2][3] = {            // 2 行 3 列
        {1, 2, 3},                 // 第 0 行
        {4, 5, 6}                  // 第 1 行
    };
    printf("第 2 行第 3 列：%d\n", table[1][2]);   // 下标同样从 0 开始，得到 6

    return 0;                      // 程序正常结束
}                                  // main 函数结束
