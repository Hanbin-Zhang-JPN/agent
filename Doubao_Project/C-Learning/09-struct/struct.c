/* ============================================================
 * 文件：09-struct/struct.c
 * 主题：结构体
 * 学习点：定义结构体 / 访问成员 / 修改成员 / 结构体数组
 * ============================================================ */

#include <stdio.h>                 // 引入标准输入输出库

// 定义结构体：把多个相关数据打包成一个新类型
struct Student {                   // struct 关键字 + 类型名
    char name[20];                 // 成员：姓名（字符数组）
    int age;                       // 成员：年龄
    float score;                   // 成员：成绩
};                                 // 注意：结构体定义结束要加分号

int main(void)
{
    // 1. 声明结构体变量并初始化，成员按定义顺序填
    struct Student stu1 = {"小明", 18, 92.5};

    // 2. 用点号 . 访问成员
    printf("姓名：%s\n", stu1.name);    // 输出：小明
    printf("年龄：%d\n", stu1.age);     // 输出：18
    printf("成绩：%.1f\n", stu1.score); // 输出：92.5

    // 3. 修改成员：和普通变量一样赋值
    stu1.age = 19;                 // 年龄改成 19
    printf("修改后年龄：%d\n", stu1.age);

    // 4. 结构体数组：一次性管理多个学生
    struct Student students[3] = {       // 数组元素类型是 Student
        {"小红", 17, 88.0},              // 第 0 个学生
        {"小刚", 18, 95.0},              // 第 1 个学生
        {"小丽", 17, 76.5}               // 第 2 个学生
    };

    // 5. 遍历结构体数组，逐个输出
    for (int i = 0; i < 3; i++) {        // i 依次取 0、1、2
        printf("%s 的成绩是 %.1f\n",     // 输出姓名和成绩
               students[i].name, students[i].score);
    }

    return 0;                      // 程序正常结束
}                                  // main 函数结束
