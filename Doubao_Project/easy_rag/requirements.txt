# ============================================================
# easy_rag 依赖声明
#
# 注意：本项目核心功能【不依赖任何第三方库】，只使用 Python 标准库。
# 下方 numpy 为【可选】项：装了它，稠密向量检索会用矩阵乘法加速；
# 不装也完全可用（代码会自动降级为纯 Python 计算，结果一致）。
#
# 安装（可选）：pip3 install -r requirements.txt
# ============================================================

numpy>=1.24          # 可选：仅用于稠密检索的批量矩阵计算加速
