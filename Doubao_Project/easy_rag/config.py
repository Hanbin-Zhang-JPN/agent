# ============================================================
# 全局配置：所有模块共享的参数都收在这里，改一处全局生效
# ============================================================

# ---- 检索参数 ----
EMBED_DIM = 2048        # 稠密向量维度：越大容纳词元越多、哈希碰撞越少
K1 = 1.5                # BM25 词频饱和参数：越大词频越"不饱和"
B = 0.75                # BM25 长度归一化：越大对长文档惩罚越重
TOP_K = 5               # 默认返回给大模型的段落条数
RRF_K = 60              # RRF 融合常数：参与混合排序时的平滑项

# ---- 切块参数 ----
CHUNK_SIZE = 500        # 每块最大字符数
CHUNK_OVERLAP = 100     # 相邻块重叠字符数，防止句子被拦腰切断

# ---- 大模型参数（任意兼容 OpenAI 协议的服务均可）----
LLM_BASE = "https://api.openai.com/v1"   # 接口根地址，需带版本路径 /v1
LLM_MODEL = "gpt-4o-mini"                # 模型名，按你实际可用的服务修改
LLM_TIMEOUT = 90                         # 单次请求超时（秒）
LLM_MAX_TOKENS = 1500                    # 单次回复最大 token 数

# 密钥不写死在代码里，从环境变量读取，更安全
# 用法：export LLM_API_KEY=sk-xxxx
import os
LLM_API_KEY = os.environ.get("LLM_API_KEY", "")
