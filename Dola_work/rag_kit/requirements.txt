# rag_kit — 依存パッケージ
#
# 本体はPython標準ライブラリのみで動作します。
# そのため「必須の依存」はありません（外部ブラックボックス非依存）。
#
# 任意（オプション）で追加できるもの:
#   pypdf    : PDF文書の読み込みに必要（pip install pypdf）
#   numpy    : 大規模コーパスでベクトル計算を高速化したい場合（任意）
#
# 用途に応じて、必要なものだけを pip install してください。
